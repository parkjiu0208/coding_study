def solution(n):
    hap=0
    if n%2==0:
        for i in range(2,n+1,2):
            hap += i**2
        return hap
    else:
        for i in range(1,n+1,2):
            hap += i
        return hap
    
print(solution(7))    
    #반복문을 2칸씩 띄어서 쓰는 것을 혼자서 생각한 것이 뿌듯하다.